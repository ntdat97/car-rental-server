name: car_rental_server
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.5.3

# Add regular dependencies here.
dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  dart_jsonwebtoken: ^2.8.0
  bcrypt: ^1.1.3
  dotenv: ^4.1.0
  mysql1: ^0.20.0
  firebase_admin: ^0.3.0
  http_parser: ^4.1.1

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
